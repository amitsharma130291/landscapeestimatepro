<figure class="overflow-hidden rounded-2xl border border-border bg-white shadow-sm">
  <picture>
    <source srcset="/screenshots/service-setup.webp" type="image/webp" />
    <img
      src="/screenshots/service-setup.png"
      width="1720"
      height="770"
      loading="lazy"
      decoding="async"
      alt="Landscape Estimate Pro's real service-assembly editor showing 'Mulch Installation' built once from a material, a labor assumption, and an other cost, with its true cost per yd³ calculated automatically."
      class="w-full"
    />
  </picture>
  <figcaption class="border-t border-border bg-paper-dim px-4 py-2 text-xs text-muted">
    Example project — the app's own default sample data. Build the service once; reuse its cost structure in every future estimate.
  </figcaption>
</figure>
